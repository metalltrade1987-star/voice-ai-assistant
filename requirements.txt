# ============================================================
# Голосовой AI-ассистент — зависимости
# Установка: pip install -r requirements.txt
# ============================================================

# ------------------------------------------------------------
# ОСНОВНЫЕ БИБЛИОТЕКИ
# ------------------------------------------------------------
requests>=2.31.0           # HTTP-запросы к Ollama, Open-Meteo
numpy>=1.24.0              # Матрицы, обработка данных
opencv-python>=4.8.0       # Компьютерное зрение, работа с камерой
Pillow>=10.0.0             # Обработка изображений

# ------------------------------------------------------------
# РАСПОЗНАВАНИЕ И СИНТЕЗ РЕЧИ (STT + TTS)
# ------------------------------------------------------------
vosk>=0.3.45               # Распознавание речи (локально)
sounddevice>=0.4.6         # Запись с микрофона
torch>=2.0.0               # Silero TTS (синтез речи)
torchaudio>=2.0.0          # Аудио-утилиты для Silero

# ------------------------------------------------------------
# КОМПЬЮТЕРНОЕ ЗРЕНИЕ (распознавание лиц)
# ------------------------------------------------------------
deepface>=0.0.79           # Распознавание лиц
tf-keras>=2.13.0           # TensorFlow для DeepFace

# ------------------------------------------------------------
# ПАМЯТЬ И БАЗЫ ДАННЫХ
# ------------------------------------------------------------
chromadb>=0.4.0            # Векторная БД для RAG (база знаний)

# ------------------------------------------------------------
# РАБОТА С ИНТЕРНЕТОМ
# ------------------------------------------------------------
duckduckgo-search>=4.0     # Поиск в DuckDuckGo
beautifulsoup4>=4.12.0     # Парсинг HTML
cloudscraper>=1.2.71       # Обход Cloudflare
trafilatura>=1.6.0         # Извлечение текста из HTML
crawl4ai>=0.9.0            # Чтение сайтов через headless-браузер

# ============================================================
# ПРИМЕЧАНИЯ:
# - sqlite3 встроен в Python (не нужен отдельный пакет)
# - Ollama используется через HTTP API (не нужен Python SDK)
# - soundfile ставится автоматически вместе с sounddevice
# ============================================================